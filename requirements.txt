# CUDA 12.4 GPU 빌드(torch/torchaudio)를 받기 위한 인덱스.
# GPU가 없다면 아래 torch/torchaudio 줄의 "+cu124"를 빼고 이 줄도 지우면 CPU 빌드가 설치됩니다.
--extra-index-url https://download.pytorch.org/whl/cu124

# ── 공통 ──────────────────────────────────────────────────────────────────────
numpy>=1.24.4
python-dotenv>=1.2.2
pydantic>=2.12.5
requests>=2.33.1

# ── STT ───────────────────────────────────────────────────────────────────────
faster-whisper>=1.2.0
websockets>=12.0
sounddevice>=0.4.6       # 마이크 테스트(MicrophoneASRTest) / 스피커 테스트(SpeakerTTSTest) 용

# ── TTS ───────────────────────────────────────────────────────────────────────
gtts>=2.5.0
pydub>=0.25.1            # MP3 → PCM 변환 (ffmpeg 필요)

# ── GPU / Torch (STT Silero-VAD · TTS · sentence-transformers 공통) ─────────
# CPU 빌드가 깔리는 것을 막기 위해 cu124 GPU 빌드로 고정.
# torch와 torchaudio 버전은 반드시 동일해야 함(ABI). cu124 채널 최신 호환쌍 = 2.6.0.
torch==2.6.0+cu124
torchaudio==2.6.0+cu124

# ── ASR Finetuning ────────────────────────────────────────────────────────────
datasets
evaluate
jiwer>=4.0.0
torchcodec>=0.11.1
accelerate
transformers

# ── LLM / LangGraph / RAG ─────────────────────────────────────────────────────
langchain>=1.2.15
langchain-openai>=1.1.12
langchain-community>=0.4.1
langchain-chroma>=1.1.0
langgraph>=1.1.6
chromadb>=1.5.7
pypdf>=6.9.2
sentence-transformers>=5.4.0
langchain-ollama>=0.1.0

# ── API 서버 ──────────────────────────────────────────────────────────────────
fastapi>=0.135.3
uvicorn>=0.44.0
