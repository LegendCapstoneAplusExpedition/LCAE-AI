faster-whisper>=1.2.0
websockets>=12.0
numpy>=1.24.4
sounddevice>=0.4.6       # 마이크 테스트(MicrophoneASRTest) / 스피커 테스트(SpeakerTTSTest) 용
python-dotenv>=1.0.0     # .env 파일 로드

# TTS
coqui-tts>=0.22.0        # Coqui TTS 엔진 (pipeline/tts)

# GPU 사용 시 (선택 — STT faster-whisper, TTS Coqui 공통)
# torch>=2.0.0

# Silero-VAD 사용 시 (선택, 더 정확한 VAD)
torch>=2.3.1
torchaudio>=2.3.1

#ASR Finetuning
datasets
evaluate
jiwer>=4.0.0
torchcodec>=0.11.1
accelerate
transformers>=4.55.0